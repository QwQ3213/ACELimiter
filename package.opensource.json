{
  "name": "ace-limiter",
  "version": "0.1.0",
  "description": "ACE Limiter - 开源版本（预编译前端）",
  "scripts": {
    "tauri:build": "cd src-tauri && cargo tauri build"
  },
  "dependencies": {
    "@tauri-apps/cli": "^2.9.6"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
